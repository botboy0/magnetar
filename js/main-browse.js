/* ============================================================
   main-browse.js
   Orchestrator for devlog.html and index.html (Games page).
   Editor has its own orchestrator at main-editor.js.
   ============================================================ */

import { init as initTopStrip } from './components/top-strip.js';

initTopStrip();